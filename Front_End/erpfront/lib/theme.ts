// Single source for the theme localStorage key. Read by the pre-hydration script in `app/layout.tsx`
// (interpolated into the inline script string) and written by `components/auth/ThemeToggle.tsx`.
export const THEME_STORAGE_KEY = "erp_theme";
